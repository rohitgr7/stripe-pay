import React from 'react';

const Header = () => (
  <div>
    <nav className="navbar navbar-dark bg-dark">
      <span className="navbar-brand mx-auto h1">Stripe Pay</span>
    </nav>
  </div>
);

export default Header;
